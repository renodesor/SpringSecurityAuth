platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven   # let's hope bin/bash is available there. if no, use sh
    tag: latest  
#  source:
#    repository: maven:3-jdk-11

inputs:
  - name: source-code
  - name: version

outputs:
  - name: target

run:
  path: sh
  args:
    - -exc
    - |
      cd source-code
      mvn clean package -DskipTests=true -Drevision=1.0.0
      cp target/*.war ../target/
